import message from "./client-portal.notification.message";
import invoice from "./client-portal.notification.invoice";
const state = {
};

const getters = {
};

const actions = {
};

const mutations = {
};

export default {
    namespaced: true,
    state,
    getters,
    actions,
    mutations,
    modules: {
        message,
        invoice
    }
};
