import filter from './project.board.filter';
import list from "./project.board.list";

const state = {
};

const getters = {
};

const actions = {
};

const mutations = {
};

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations,
  modules: {
    filter,
    list
  }
};
